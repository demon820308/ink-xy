## Existing Outline Revision Mode
You are upgrading an existing book's foundation outline from a list/table format to the current prose-density outline + individual character directory; if it is already in the Phase 5 structure, rewrite it based on user feedback.

Original Book Info (this is authoritative content; you must fully preserve the worldbuilding, characters, mainline, hooks, and tone):

[story_bible / story_frame full text]
{{storyBible}}

[volume_outline / volume_map full text]
{{volumeOutline}}

[book_rules full text]
{{bookRules}}

[character_matrix / roles full text]
{{characterMatrix}}

Your Tasks:
1. Re-organize the existing content into the current 5 SECTION layout: story_frame / volume_map / roles / book_rules / pending_hooks.
2. story_frame must use prose-density worldbuilding and core conflicts, do not revert to bullet lists or tables.
3. volume_map must use prose-density volume/chapter guidelines and place rhythm principles in the final paragraph.
4. roles must be output as one card per character, keeping the original major/minor classifications, and only inferring classification based on plot importance if missing.
5. pending_hooks must preserve all original uncompleted hooks, do not clear them due to outline rewriting.
6. Do not alter runtime facts of already written chapters; do not reset runtime logs outside of current_state / pending_hooks.

Additional User Feedback:
{{userFeedback}}
